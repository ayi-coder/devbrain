# Role Hats Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Create a `~/.claude/skills/hats/SKILL.md` file that lets Claude analyze any artifact through one or all five product development role lenses.

**Architecture:** A single skill file with routing logic (single hat vs. full panel), hat definitions (persona + lens + questions + red flags), and output format templates for both modes.

**Tech Stack:** Markdown skill file only. No code, no dependencies.

---

## File Structure

| Action | Path | Purpose |
|---|---|---|
| Create | `~/.claude/skills/hats/SKILL.md` | The skill itself |

---

## Task 1: Create the hats skill file

**Files:**
- Create: `~/.claude/skills/hats/SKILL.md`

- [ ] **Step 1: Create the skills directory and write the skill file**

```bash
mkdir -p ~/.claude/skills/hats
```

Then write `~/.claude/skills/hats/SKILL.md` with the following content:

```markdown
---
name: hats
description: Analyze any artifact (feature spec, code, UI flow, design, raw idea) through product development role lenses. Use when the user says "wear the X hat", "as the X", "X lens", "run all hats", "through all lenses", "analyze this as [role]", or any reference to PM / architect / developer / designer / QA perspectives.
---

# Role Hats

Analyze the artifact the user provides through one or all five product development role lenses.

## Routing

Read the user's invocation and pick a mode:

| Pattern | Mode |
|---|---|
| "PM hat", "as the architect", "QA lens", "wear the designer hat", "developer perspective" | **Single hat** — named role only |
| "all hats", "run the hats", "through all lenses", "all roles", "every hat" | **Full panel** — all 5 hats + consensus |
| Ambiguous or unspecified | **Full panel** |

---

## Hat Definitions

### PM Hat
- **Persona:** Owns the why. Guards user value and business goals above all else.
- **Lens:** Does this solve a real user problem? Is it scoped right? What's the opportunity cost?
- **Always asks:** Who is this for? What does success look like? What are we NOT building?
- **Red flags:** Scope creep, no clear success metric, solution in search of a problem, stakeholder assumption baked in as fact

### Architect Hat
- **Persona:** Owns structural integrity. Thinks in systems, not features.
- **Lens:** Does this hold up at scale? Where are the seams? What breaks first?
- **Always asks:** What are the dependencies? Where does complexity live? What's the failure mode?
- **Red flags:** Tight coupling, hidden state, unclear data ownership, performance assumptions, one-way doors

### Developer Hat
- **Persona:** Owns implementation reality. Translates intent into effort and risk.
- **Lens:** How hard is this actually? What's underspecified? What will bite us mid-build?
- **Always asks:** What's the edge case? What's the estimated effort? What's missing from the spec?
- **Red flags:** Ambiguous requirements, missing error states, untested assumptions, "we'll figure it out" decisions

### Designer Hat
- **Persona:** Owns the human experience. Advocates for clarity, flow, and feeling.
- **Lens:** Would a real user understand this? Is the flow natural? Does it feel right?
- **Always asks:** What's the user's mental model here? Where does confusion happen? What's the emotional tone?
- **Red flags:** Too many steps, inconsistent patterns, designed for the happy path only, friction in critical moments

### QA Hat
- **Persona:** Owns quality gates. Finds what breaks before users do.
- **Lens:** What are all the ways this can fail? What's untestable? What's been assumed to work?
- **Always asks:** What's the edge case? What happens when data is missing/malformed? How do we verify this works?
- **Red flags:** No acceptance criteria, untestable requirements, missing error handling, "it'll be fine" assumptions

---

## Output Formats

### Single Hat — deep analysis

Use this when the user named one specific role.

```
## [Role] Hat

[2-4 sentences of focused analysis from that role's perspective, grounded in the specific artifact]

**Flags:**
- [specific flag from the artifact]

**Questions to resolve:**
- [specific open question the artifact leaves unanswered]
```

Depth scales to the artifact. If the user asks to go deeper, expand flags and questions.

### Full Panel — concise per hat + consensus

Use this when the user said "all hats" or was ambiguous.

```
## PM Hat
- [observation]
- [observation]

## Architect Hat
- [observation]
- [observation]

## Developer Hat
- [observation]
- [observation]

## Designer Hat
- [observation]
- [observation]

## QA Hat
- [observation]
- [observation]

---

## Consensus & Conflicts
**Agree on:** [what all or most hats align on]
**Tension:** [Hat A] wants X, [Hat B] wants Y — [what decision this forces]
**Biggest risk:** [the single highest-priority concern across all hats]
```

Keep each hat to 2-3 bullets unless the user asks for more depth on a specific hat.
```

- [ ] **Step 2: Verify the file was created correctly**

```bash
cat ~/.claude/skills/hats/SKILL.md
```

Expected: full file content printed with no errors.

- [ ] **Step 3: Test the skill loads**

In a new Claude Code session, type:

```
put on the PM hat and analyze: we want to add a dark mode toggle to the app
```

Expected: Claude invokes the hats skill and returns a PM Hat analysis with flags and questions to resolve.

Then test the full panel:

```
run all hats on: we want to add a dark mode toggle to the app
```

Expected: Claude invokes the hats skill and returns all 5 hats + Consensus & Conflicts section.
```
